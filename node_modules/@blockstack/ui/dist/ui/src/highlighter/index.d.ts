import React from 'react';
import { Language } from './types';
import './language-definition';
export interface HighlighterProps {
    code: string;
    language?: Language;
    showLineNumbers?: boolean;
}
export declare const Highlighter: React.MemoExoticComponent<({ code, language, showLineNumbers }: HighlighterProps) => JSX.Element>;
