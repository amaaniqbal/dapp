import * as React from 'react';
import * as StyledSystem from 'styled-system';
import { PseudoBoxProps } from '../pseudo-box';
export declare type ButtonSizes = 'md' | 'lg';
export declare type ButtonColorVariants = string;
export declare type ButtonVariants = 'outline' | 'unstyled' | 'link' | 'solid';
export declare type ButtonModes = 'primary' | 'secondary';
export interface CustomStyles {
    primary: PseudoBoxProps;
    secondary: PseudoBoxProps;
}
interface ButtonPropsBase {
    size?: ButtonSizes;
    isLoading?: boolean;
    variantColor?: ButtonColorVariants;
    variant?: ButtonVariants;
    mode?: ButtonModes;
    customStyles?: CustomStyles;
    isActive?: boolean;
    isDisabled?: boolean;
    loadingText?: string;
    isFullWidth?: boolean;
    type?: 'button' | 'reset' | 'submit';
    children: React.ReactNode;
    leftIcon?: string;
    rightIcon?: string;
    iconSpacing?: StyledSystem.MarginProps['margin'];
}
export interface ButtonStyles {
    variant: ButtonVariants;
    mode: ButtonModes;
    size: ButtonSizes;
    customStyles?: CustomStyles;
}
export declare type ButtonProps = ButtonPropsBase & PseudoBoxProps & React.RefAttributes<HTMLButtonElement>;
export {};
