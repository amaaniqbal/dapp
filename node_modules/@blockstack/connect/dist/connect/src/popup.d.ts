interface PopupOptions {
    url?: string;
    title?: string;
    w?: number;
    h?: number;
}
export declare const popupCenter: ({ url, title, w, h, }: PopupOptions) => Window | null;
export {};
