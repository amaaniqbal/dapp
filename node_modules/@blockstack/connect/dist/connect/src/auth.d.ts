import { UserSession } from 'blockstack';
import './types';
export interface FinishedData {
    authResponse: string;
    userSession: UserSession;
}
export interface AuthOptions {
    redirectTo?: string;
    manifestPath?: string;
    finished?: (payload: FinishedData) => void;
    authOrigin?: string;
    sendToSignIn?: boolean;
    userSession?: UserSession;
    appDetails: {
        name: string;
        icon: string;
    };
}
export declare const authenticate: ({ redirectTo, manifestPath, finished, authOrigin, sendToSignIn, userSession, appDetails, }: AuthOptions) => Promise<void>;
